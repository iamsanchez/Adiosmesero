<h1>Listing tables</h1>

<table>
  <thead>
    <tr>
      <th>In use?</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>

    <% @tables.each do |table| %>
      <tr>
        <% if table.used %>
        <td><%= 'En orden' %></td>
        <td><%= link_to 'Show', table %></td>
        <% end %>

        </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Table', new_table_path %>
